/// <reference types="vite/client" />

/** Build timestamp, stamped in by vite.config.ts. Shown in Settings so the
    installed version can be identified from the phone itself. */
declare const __BUILT_AT__: string;
